#include <iostream>
#include <map>
#include <vector>
#include <algorithm>
#include <tuple>
#include <stack>
#include <string>

using namespace std;

// Generando la poblacion inicial:
vector<vector<string>> p_inicial;
void generar_poblacion(int t_poblacion){
       
}

int main(){
    cout<<"h"<<endl;
    int t_poblacion = 4; // tamano de la poblacion
    int t_cromosomas = 8; // tamano de cromosomas
    int iteraciones = 30; // iteraciones
    float p_cruzamiento = 0.9; // probabilidad de cruzamiento
    int punto = 3; // Utilizo el cruzamiento de un punto
    float p_mutacion = 0.05; // Probabilidad de mutación
    //funcion(t_poblacion, t_cromosomas, iteraciones, p_cruzamiento, punto, p_mutacion);
    return 0;
}
// g++ -std=c++11 your_file.cpp -o a
// ./a